enum DbMessageEnum { SUCCESS, PENDING, FAILED }

enum DbStatusEnum { SUCCESS, PENDING, FAILED }

enum JobSourceEnum { OLD, NEW }

enum JobStatusEnum { CANCELED, OVERDUED, UNABLE, IN_PROGRESS, FAILED, DONE }

enum UserRoleEnum { USER, ADMIN, MANAGER }
